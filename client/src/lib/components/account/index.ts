import ProfileSettings from './profile-settings.svelte';
import EmailSettings from './email-settings.svelte';
import PasswordSettings from './password-settings.svelte';

export { ProfileSettings, EmailSettings, PasswordSettings };
